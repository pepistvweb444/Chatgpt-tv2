name: Build Jarvis TV APK

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'Jarvis-TV-v0.2-debug.apk.b64'
      - 'LATEST_RUN_ID.txt'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: jarvis-tv-apk-main
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK 35
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK and capture log
        id: build_apk
        shell: bash
        run: |
          set +e
          gradle --no-daemon --stacktrace :app:assembleDebug > build.log 2>&1
          status=$?
          tail -n 250 build.log > build-last-250-lines.txt
          echo "exit_code=$status" >> "$GITHUB_OUTPUT"
          cat build-last-250-lines.txt
          exit $status

      - name: Persist failure log in repository
        if: failure() && steps.build_apk.outputs.exit_code != '0'
        shell: bash
        run: |
          cp build-last-250-lines.txt BUILD_FAILURE_LOG.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add BUILD_FAILURE_LOG.txt
          git commit -m "ci: persist latest Android build failure [skip ci]" || true
          git push

      - name: Rename APK
        if: success()
        run: cp app/build/outputs/apk/debug/app-debug.apk Jarvis-TV-v0.2-debug.apk

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Jarvis-TV-v0.2-debug
          path: Jarvis-TV-v0.2-debug.apk
          if-no-files-found: error

      - name: Publish APK release
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh release view v0.2 >/dev/null 2>&1; then
            gh release upload v0.2 Jarvis-TV-v0.2-debug.apk --clobber
          else
            gh release create v0.2 Jarvis-TV-v0.2-debug.apk --title "Jarvis TV v0.2" --notes "Android TV debug APK generated automatically by GitHub Actions."
          fi

      - name: Publish APK payload and run ID
        if: success()
        shell: bash
        run: |
          base64 -w 0 Jarvis-TV-v0.2-debug.apk > Jarvis-TV-v0.2-debug.apk.b64
          echo "${{ github.run_id }}" > LATEST_RUN_ID.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Jarvis-TV-v0.2-debug.apk.b64 LATEST_RUN_ID.txt
          git commit -m "build: publish Jarvis TV v0.2 APK payload [skip ci]" || true
          git push
